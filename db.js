// const mongoose = require("mongoose");

// mongoose.connect("mongodb://127.0.0.1:27017/trail", { useNewUrlParser: true });

// const dbconn = mongoose.connection;

// dbconn.on("error", console.error.bind(console, "connection error:"));
// dbconn.once("open", function () {
//   const connectedDatabase = mongoose.connection.name;
//   const connectedPort = mongoose.connection.port;

  

//   console.log(
//     `Connected to MongoDB successfully. Connected to database: ${connectedDatabase} on port: ${connectedPort}`
//   );
// });